### Task 1 — Build & test tooling bootstrap

Set up the project so it is “alive”:
[x] Use the Yarn v4+ (Berry) package manager
[x] Vite dev server running
[x] Modern TypeScript configured (JSX, etc)
[x] Vitest configured (and at least one basic test)
[x] Basic scripts in `package.json` to run dev/build/test
[x] Create `yarn precheck` script that runs linting + typechecking + tests
[x] Minimal CI-friendly structure and Github actions workflow file (setup repo, node, run unit tests; no app features required yet)

Only when the above is done, this task can be considered complete. Subsequent tasks will build on top of this foundation.
